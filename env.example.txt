# Renommez ce fichier en .env.local (Next.js) et/ou .env (Prisma).
# Le .env est ignoré par git via .gitignore.

# -----------------------------------------------------------------------------
# Database — Neon PostgreSQL
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://neondb_owner:npg_hiCVONIRT69f@ep-dry-feather-apnx5yev-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# -----------------------------------------------------------------------------
# Auth (JWT)
# -----------------------------------------------------------------------------
# Générer une clé : `openssl rand -base64 48`
AUTH_JWT_SECRET="change-me-with-a-strong-random-string-of-at-least-32-chars"
AUTH_ACCESS_TOKEN_TTL="15m"        # durée access token
AUTH_REFRESH_TOKEN_TTL_DAYS="30"   # durée refresh token / session
AUTH_COOKIE_DOMAIN=""              # vide en dev ; prod : .hotelnahoui.ci
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# Resend (email OTP + transactional)
# -----------------------------------------------------------------------------
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Hôtel Nahoui <noreply@hotelnahoui.ci>"
# Email(s) qui reçoivent les notifications de nouvelles réservations (séparés par des virgules)
ADMIN_NOTIFICATION_EMAIL=""

# -----------------------------------------------------------------------------
# TPECloud (SMS Côte d'Ivoire)
# Documentation : https://tpecloud.com
# -----------------------------------------------------------------------------
TPECLOUD_API_URL="https://api.tpecloud.com/sms/send"
TPECLOUD_API_KEY=""
TPECLOUD_SENDER_ID="NAHOUI"

# -----------------------------------------------------------------------------
# Google OAuth
# https://console.cloud.google.com/apis/credentials
# Redirect URI web    : {APP_URL}/api/auth/oauth/google/callback
# Redirect URI mobile : com.hotelnahoui.app:/oauth2redirect (Flutter)
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# Facebook OAuth
# https://developers.facebook.com/apps
# -----------------------------------------------------------------------------
FACEBOOK_CLIENT_ID=""
FACEBOOK_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# OTP
# -----------------------------------------------------------------------------
OTP_TTL_MINUTES="10"
OTP_MAX_ATTEMPTS="5"
OTP_LENGTH="6"
# En dev, log l'OTP dans la console au lieu de l'envoyer
OTP_DEBUG="true"

# -----------------------------------------------------------------------------
# Payment Providers
# -----------------------------------------------------------------------------
# Options: 'fineopay' | 'centralapis'
PAYMENT_PROVIDER="fineopay"

# -----------------------------------------------------------------------------
# FineoPay
# -----------------------------------------------------------------------------
FINEOPAY_API_URL="https://api.fineopay.com/api/v1/business/dev/checkout-link"
FINEOPAY_BUSINESS_CODE=""
FINEOPAY_API_KEY=""
FINEOPAY_CALLBACK_URL=""

# -----------------------------------------------------------------------------
# CENTRAL APIS
# -----------------------------------------------------------------------------
CENTRAL_APIS_API_URL="https://api.centralapis.com/api/v1/finance"
CENTRAL_APIS_CLIENT_ID=""
CENTRAL_APIS_CLIENT_SECRET=""
CENTRAL_APIS_CALLBACK_URL=""
